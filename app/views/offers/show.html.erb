<%
 if @offer.assignment == nil
  noteButton = "offer"
 end
%>
<% if @offer.customer == current_user and @offer.assignment == nil %>
<div><%= link_to "Accept", convert_assignments_path(:offer_id => @offer.id), :class => "btn btn-success" %></div>
<% end %>

<%= showObject(@offer, noteButton, @offer.request, true, true, true, params[:format]).html_safe %>