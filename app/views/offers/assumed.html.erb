<h1>My Offers</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Customer</th>
      <th>Subject</th>
      <th>Notes</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @offers.each do |offer| %>
      <tr>
        <td><%= offer.customer.email %></td>
        <td><%= offer.subject %></td>
        <td><%= offer.request.notes.count + offer.notes.count %></td>
        <td><%= link_to 'Show', offer, :class => "btn btn-info" %></td>
        <td><%= link_to 'Release', release_offer_path(offer), :class => "btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>