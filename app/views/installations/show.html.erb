<%= controlLink(@installation.order, @installation.invoice).html_safe %>
<%
if @installation.invoice == nil
	noteButton = "installation"
end
%>

<%= showObject(@installation, noteButton, @installation.order.assignment.offer.request).html_safe %>