<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "CompanyPortal" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>

    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a style="text-decoration: none;" class="brand" href="<%= root_path %>">CompanyPortal</a>
          <div class="container-fluid nav-collapse">
            <ul class="nav">
      </li>
                     
  <% if current_user %>                
      <li>      
  		  <div class="dropdown">
  		    <button class="btn dropdown-toggle sr-only" type="button" id="dropdownMenu1" data-toggle="dropdown">
           		<%= current_user.firstname %> <%= current_user.lastname %>
           		<span class="caret"></span>
           		</button>
           		<ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
 		<li><%=link_to 'Edit profile', edit_user_registration_path  %></li    
           	  </ul>
          	 </div>
		  </li>      

                <li>
		  <% if can?(:manage, Reporting) %>
                  <div class="dropdown">
                    <a href="<%= reporting_path%>">
                      <button class="btn dropdown-toggle sr-only" type="button">Reporting</button>
                    </a>
                  </div>
		  <% end %>
	          
                </li>
                <li>
		<% if can?(:open, Bugreport) %>
                <div class="dropdown">
                  <button class="btn dropdown-toggle sr-only" type="button" id="dropdownMenu1" data-toggle="dropdown">
                    Bugreports
                    <span class="caret"></span>
                  </button>
                  <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
                    <li><%= link_to "New bugreport", new_bugreport_path %></li>
                    <li class="divider"></li>
		    <% if can?(:manage, Bugreport) %>
                    <li><%= link_to "Show open bugreports (#{Bugreport.where('closed IS NULL').count})", unassumed_bugreports_path %></li>
                    <li><%= link_to "Show unassumed bugreports (#{Bugreport.where('agent_id IS NULL and closed IS NULL').count})", unassumed_bugreports_path %></li>
                    <li><%= link_to "Show my bugreports (#{Bugreport.where(agent_id: current_user.id).where("closed IS NULL").count})", assumed_bugreports_path %></li>
		    
                    <li class="divider"></li>
                    <li><%= link_to "Archive", closed_bugreports_path %></li>
		    <% end %>
                  </ul>
                </div>
		<% end %>
              </li>
              <li>
                <div class="dropdown">
                  <button class="btn dropdown-toggle sr-only" type="button" id="dropdownMenu1" data-toggle="dropdown">
                    Businessproccess
                    <span class="caret"></span>
                  </button>
                  <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
                    <li>
                      <a href="<%= customer_overview_index_path %>">Overview</a>
                    </li>
                    <li class="divider"></li>
		    <% if can?(:read, Request) %>
                    <li class="dropdown-submenu">
                      <a tabindex="-1" href="#">Requests</a>
                      <ul class="dropdown-menu">
                        <li><%= link_to "New request", new_request_path %></li>
                        <% if can?(:assume, Request) %>
                        <li class="divider"></li>
                        <li><%= link_to "Show open requests(#{Request.where('offer_id IS NULL').count})", open_requests_path %></li>
                        <li><%= link_to "Show unassumed requests (#{Request.where('agent_id IS NULL and offer_id IS NULL').count})", unassumed_requests_path %></li>
                        <li><%= link_to "Show my requests (#{Request.where(agent_id: current_user.id).where('offer_id IS NULL').count})", assumed_requests_path %></li>
                      </ul>
                    </li>
		    <% end %>
		    <% if can?(:manage, Offer) %>
                    <li class="dropdown-submenu">
                      <a tabindex="-1" href="#">Offers</a>
                      <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
                        <li><%= link_to "Pending offers(#{Offer.where('assignment_id IS NULL AND publication IS NOT NULL').count})", pending_offers_path %></li>
                        <li class="divider"></li>
                        <li><%= link_to "Show open offers(#{Offer.where('assignment_id IS NULL AND publication IS NULL').count})", open_offers_path %></li>
                        <li><%= link_to "Show unassumed offers (#{Offer.where('agent_id IS NULL and assignment_id IS NULL AND publication IS NULL').count})", unassumed_offers_path %></li>
                        <li><%= link_to "Show my offers (#{Offer.where(agent_id: current_user.id).where('assignment_id IS NULL AND publication IS NULL').count})", assumed_offers_path %></li>
                        <% end %>
                      </ul>
                    </li>
		    <% end %>
		    <% if can?(:manage, Assignment) %>
                    <li class="dropdown-submenu">
                      <a tabindex="-1" href="#">Assignments</a>
                      <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
                        <li><%= link_to "Show open assignments(#{Assignment.where('order_id IS NULL').count})", open_assignments_path %></li>
                        <li><%= link_to "Show unassumed assignments (#{Assignment.where('agent_id IS NULL and order_id IS NULL').count})", unassumed_assignments_path %></li>
                        <li><%= link_to "Show my assignments (#{Assignment.where(agent_id: current_user.id).where('order_id IS NULL').count})", assumed_assignments_path %></li>
                      </ul>
                    </li>
		    <% end %>
		    <% if can?(:manage, Order) %>
                    <li class="dropdown-submenu">
                      <a tabindex="-1" href="#">Orders</a>
                      <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
                        <li><%= link_to "Show open orders(#{Order.where('installation_id IS NULL').count})", orders_path %></li>
                      </ul>
                    </li>   
	      <% end %>   
		    <% if can?(:manage, Installation) %>          
                    <li class="dropdown-submenu">
                      <a tabindex="-1" href="#">Installations</a>
                      <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
                        <li><%= link_to "Show open installations(#{Installation.where('invoice_id IS NULL').count})", open_installations_path %></li>
          <% if can?(:assume, Installation) %>
                        <li><%= link_to "Show unassumed installations (#{Installation.where('agent_id IS NULL and invoice_id IS NULL').count})", unassumed_installations_path %></li>
                        <li><%= link_to "Show my installations (#{Installation.where(agent_id: current_user.id).where('invoice_id IS NULL').count})", assumed_installations_path %></li>
                      <% end %>
                    </ul>
                    </li>
		    <% end %>
		    <% if can?(:manage, Invoice) %> 
                    <li class="dropdown-submenu">
                      <a tabindex="-1" href="#">Invoices</a>
                      <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
                        <li><%= link_to "Show open invoices(#{Invoice.where('payed IS NULL').count})", invoices_path %></li>
                      </ul>
                    </li>
                    <% end %>
        <% if can?(:index, Archive) %>
                    <li class="divider"></li>
                    <li>
                      <a href="<%= archive_index_path %>">Archive</a>
                    </li>
        <% end %>
                  </ul>
                </div>
                
  		  <li>     
		  <% if can?(:manage, Article) %>  
  		  <div class="dropdown">
  		    <button class="btn dropdown-toggle sr-only" type="button" id="dropdownMenu1" data-toggle="dropdown">
           		Articles
           		<span class="caret"></span>
           		</button>
           		<ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
                	<li><%= link_to 'Show Article', articles_path  %></li>  
                        <li><%= link_to 'New Article', new_article_path %></li>
           	        </ul>
          	  </div>
		  <% end %>
		  </li>
              <% end %>
            </ul>

            <div class="pull-right" style="display: inline; float: right;"><table><tr>
            <%= form_for(:user, :url => user_show_path(), :method => "get") do |f| %>
            <td><%= f.text_field :firstname, :placeholder => "Search Name" %></td>
            <td><%= f.button :type => "submit", :class => "btn btn-success" do %>Search<% end %></td>
            <% end %>
            </tr></table></div>


            <% if current_user %>
              <div class="pull-right"><%=link_to 'Sign out', destroy_user_session_path, method: :delete, :class => "btn btn-danger fa fa-sign-out"  %></div>
              </div>
            <% else %>
              <div class="pull-right" style="display: inline; float: right;"><table><tr>
            <%= form_for(:user, :url => session_path(:user)) do |f| %>
            <td><%= f.text_field :email, :placeholder => "Username" %></td>
            <td><%= f.password_field :password, :placeholder => "Password" %></td>
            <td><%= f.button :type => "submit", :class => "btn btn-success" do %>Sign in <i class="fa fa-sign-in"></i><% end %></td>
            <% end %>
            <td><%=link_to new_user_registration_path  , :class => "btn btn-info", :id => "signup" do %>Sign Up <i class="fa fa-share" style="display: inline-block;"></i<% end %></td>
            </tr></table></div>
            <% end %>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">
          <%= bootstrap_flash %>
          <%= yield %>
      </div><!--/row-->

      <footer>
        <p>&copy; CompanyPortal 2014</p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
